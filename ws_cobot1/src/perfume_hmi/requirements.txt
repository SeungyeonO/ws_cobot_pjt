# perfume_hmi — pip 의존성 (pip install -r requirements.txt)
#
# 여기에는 pip으로 설치하는 서드파티만 적는다. ROS2 쪽 의존성(rclpy,
# ament_index_python, dsr_msgs2, sensor_msgs, std_msgs)은 pip 대상이 아니라
# ROS Humble 설치 + ws_dsr 워크스페이스 빌드/소싱으로 해결된다 (package.xml 참고).
# sqlite3(제조 이력 저장)는 파이썬 표준 라이브러리라 설치 불필요.

# 관리자 HMI 웹 서버 (Ubuntu 22.04 apt python3-flask는 2.0.x)
flask>=2.0
